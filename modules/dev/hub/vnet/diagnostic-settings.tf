#--------------------------------------------------------------
# Diagnostic Settings for Hub VNet Resources
#--------------------------------------------------------------
# 진단 설정은 루트 모듈(main.tf)에서 관리하여 모듈 간 순환 의존성을 제거합니다.
# 이렇게 하면 hub_vnet 모듈을 storage 모듈 없이도 독립적으로 배포할 수 있습니다.
#
# 진단 설정이 필요한 경우, 루트 모듈에서 다음과 같이 관리합니다:
# - VPN Gateway 진단 설정
# - NSG Flow Logs
# - VNet 진단 설정
#
# 각 모듈은 자신이 생성한 리소스만 관리하고,
# 진단 설정은 루트 모듈에서 통합 관리합니다.
