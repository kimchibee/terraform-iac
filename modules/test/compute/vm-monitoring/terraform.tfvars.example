#--------------------------------------------------------------
# Monitoring VM Instance - 예시 값
#--------------------------------------------------------------

vm_name = "test-x-x-vm"
vm_size = "Standard_B2s"

location             = "Korea Central"
resource_group_name  = "test-x-x-rg"

# VNet 및 서브넷 정보
vnet_name                = "test-x-x-vnet"
vnet_resource_group_name = "test-x-x-rg"
subnet_name              = "Monitoring-VM-Subnet"  # 서브넷 변경 시 이 값만 수정

# 관리자 계정
admin_username = "azureadmin"
admin_password = "YourSecurePassword123!"  # 실제 비밀번호로 변경

# SSH 키 (선택사항, 비밀번호 대신 사용)
# admin_ssh_keys = [
#   {
#     username   = "azureadmin"
#     public_key = "ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAAB..."
#   }
# ]

# 태그
tags = {
  Environment = "test"
  ManagedBy   = "Terraform"
  Project     = "test"
  Purpose     = "Monitoring"
}
