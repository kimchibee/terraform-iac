#--------------------------------------------------------------
# Storage Module - terraform.tfvars 예시
# 환경별로 이 파일을 복사하여 실제 값으로 수정
#--------------------------------------------------------------

# Storage Account 목록 (환경별로 추가/삭제 가능)
# 주의: 이 변수를 설정하면 기본 목록을 완전히 대체합니다.
# 기본 목록을 유지하면서 추가하려면 locals.tf를 수정하세요.
# storage_accounts = {
#   "vpnglog"  = "testvpnglog"     # 간단한 형태: 키 = 이름
#   "kvlog"    = "testkvlog"
#   # 또는 객체 형태:
#   # "newlog" = {
#   #   name = "testnewlog"
#   # }
# }

# 기존 리소스 재사용 (선택사항)
# existing_storage_account_name = "existing-storage-account"
# existing_key_vault_name      = "existing-key-vault"
