project_name = "test"

tags = {
  Environment = "dev"
  ManagedBy   = "Terraform"
}

spoke_subscription_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# backend_resource_group_name  = "..."
# backend_storage_account_name = "..."
# backend_container_name       = "tfstate"

# Hub 전용 방화벽만 쓰는 경우 그대로 false (Policy만 Spoke RG에 생성)
# deploy_azure_firewall = true
# firewall_subnet_key   = "AzureFirewallSubnet"
