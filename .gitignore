# Terraform
.terraform/
# 공통 모듈은 terraform-modules 레포에서만 관리. 로컬 복사본이 있어도 커밋하지 않음.
terraform_modules/
*.tfstate
*.tfstate.*
maintfstatestorage
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
tfplan
*.tfplan

# tfvars (민감 정보: 구독 ID, backend 이름 등)
*.tfvars
!terraform.tfvars.example

# Backend 설정 (스토리지 계정·RG 이름 등 환경별 값 포함)
**/backend.hcl
# Compute stack: Terraform이 생성한 VM SSH 개인키 (절대 커밋 금지)
**/monitoring_vm_key.pem
**/vm_key.pem
**/linux_monitoring_vm_key.pem

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Temporary files
*.json.bak
*-resources*.json
comparison-report.json

# Shell scripts (테스트/체크용만 제외; scripts/generate-backend-hcl.sh 는 커밋 유지)
# *.sh
maintfstatestorage

# 문서 (로컬/원격 공통 제외)
docs/
